# HubSpot Deals Client (React + TypeScript)

Run locally:

```bash
cd hubspot-deals-app/client
npm install
npm run dev
```

The client proxies `/api` to `http://localhost:3000` by default; run the server side concurrently.
